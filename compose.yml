name: petstore-modernized

x-app-common: &app-common
  build:
    context: .
  environment:
    DEMO_USERNAME: ${DEMO_USERNAME:-alice}
    DEMO_PASSWORD: ${DEMO_PASSWORD:-petstore-demo}
    DEMO_ADDITIONAL_USERNAME: ${DEMO_ADDITIONAL_USERNAME:-aditya}
    DEMO_ADDITIONAL_PASSWORD: ${DEMO_ADDITIONAL_PASSWORD:-password}
    ADMIN_USERNAME: ${ADMIN_USERNAME:-admin}
    ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin}
    LOG_FILE: /app/logs/petstore.log
  volumes:
    - app-logs:/app/logs
  healthcheck:
    test: ["CMD", "curl", "--fail", "--silent", "http://localhost:8080/actuator/health/readiness"]
    interval: 10s
    timeout: 5s
    retries: 12
    start_period: 20s

services:
  oracle:
    image: ghcr.io/gvenzl/oracle-free:23.26.2-slim
    profiles: ["oracle"]
    environment:
      ORACLE_PASSWORD: ${ORACLE_ADMIN_PASSWORD:-oracle_local_admin}
      APP_USER: ${ORACLE_USERNAME:-petstore}
      APP_USER_PASSWORD: ${ORACLE_PASSWORD:-petstore_local_only}
    ports:
      - "${ORACLE_PORT:-1521}:1521"
    volumes:
      - oracle-data:/opt/oracle/oradata
    healthcheck:
      test: ["CMD", "healthcheck.sh"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s

  mongo:
    image: mirror.gcr.io/library/mongo:8.2.11
    profiles: ["mongo"]
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all"]
    ports:
      - "${MONGO_PORT:-27017}:27017"
    volumes:
      - mongo-data:/data/db
    healthcheck:
      test: ["CMD-SHELL", "mongosh --quiet --eval 'try { rs.status().ok } catch (e) { rs.initiate({_id:\"rs0\",members:[{_id:0,host:\"mongo:27017\"}]}).ok }' | grep 1"]
      interval: 5s
      timeout: 10s
      retries: 30
      start_period: 10s

  app-oracle:
    <<: *app-common
    profiles: ["oracle"]
    environment:
      APP_STORE: oracle
      ORACLE_URL: jdbc:oracle:thin:@oracle:1521/FREEPDB1
      ORACLE_USERNAME: ${ORACLE_USERNAME:-petstore}
      ORACLE_PASSWORD: ${ORACLE_PASSWORD:-petstore_local_only}
      DEMO_USERNAME: ${DEMO_USERNAME:-alice}
      DEMO_PASSWORD: ${DEMO_PASSWORD:-petstore-demo}
      DEMO_ADDITIONAL_USERNAME: ${DEMO_ADDITIONAL_USERNAME:-aditya}
      DEMO_ADDITIONAL_PASSWORD: ${DEMO_ADDITIONAL_PASSWORD:-password}
      ADMIN_USERNAME: ${ADMIN_USERNAME:-admin}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin}
      LOG_FILE: /app/logs/petstore.log
    ports:
      - "${APP_PORT:-8080}:8080"
    depends_on:
      oracle:
        condition: service_healthy

  app-mongo:
    <<: *app-common
    profiles: ["mongo"]
    environment:
      APP_STORE: mongo
      MONGODB_URI: mongodb://mongo:27017/petstore?replicaSet=rs0
      DEMO_USERNAME: ${DEMO_USERNAME:-alice}
      DEMO_PASSWORD: ${DEMO_PASSWORD:-petstore-demo}
      DEMO_ADDITIONAL_USERNAME: ${DEMO_ADDITIONAL_USERNAME:-aditya}
      DEMO_ADDITIONAL_PASSWORD: ${DEMO_ADDITIONAL_PASSWORD:-password}
      ADMIN_USERNAME: ${ADMIN_USERNAME:-admin}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin}
      LOG_FILE: /app/logs/petstore.log
    ports:
      - "${APP_PORT:-8080}:8080"
    depends_on:
      mongo:
        condition: service_healthy

volumes:
  oracle-data:
  mongo-data:
  app-logs:
